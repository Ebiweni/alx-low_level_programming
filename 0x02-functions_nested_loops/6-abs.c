#include "main.h"

/**
 * _abs - compute absolute value of a number
 * @e: character to be checked
 * Return: 0
 */

int _abs(int e)
{
	if (e >= 0)
	{
		return (e);
	}
	else
	{
		return (e * -1);
	}
}

